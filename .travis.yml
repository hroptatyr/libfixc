language: c

compiler:
  - gcc
  - clang

before_script:
  - autoreconf -vfi

script: ./configure && make clean all check distcheck

## whitelist
branches:
  only:
    - master
    - next

notifications:
  email:
    - devel@fresse.org
